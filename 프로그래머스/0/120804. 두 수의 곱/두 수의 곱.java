class Solution {
    public int solution(int num1, int num2) {
        int answer = 0;
        
        // 1. num1과 num2를 곱하고, answer에 값을 할당한다.
        answer = num1 * num2;
        
        return answer;
    }
}